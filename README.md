
```
> apt install python3-pip
> pip3 install virtualenv
> git clone github.com/harrisonturton/teamtalk
> virtualenv teamtalk
> cd teamtalk
> source bin/activate
> pip install -r requirements.txt
```

```
pip3 install flask slackclient python-dotenv
```
